from django.apps import AppConfig


class EmailContactConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'email_contact'
    verbose_name = 'Список контактов подписки(email)'
